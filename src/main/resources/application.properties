spring.application.name=demo
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/springboot_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:springuser}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Spring@2026#Boot}
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto= update

# App Properties
demo.app.jwtSecret= ======================BezKoder=Spring===========================
demo.app.jwtExpirationMs=86400000
